<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bootstrap demo</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
    <style>
    .nav{
        display: flex;
        justify-content: space-evenly;
        margin: 50px 50px;
        text-align: center;
    } 

    body{
        font-family: sans-serif;
        font-size: 22px;
    }

    a{
        text-decoration: none!important;
        color: black;
      
    }

    a:hover{
        transition: ease-in-out;
        color: green;
    }
    </style>
    
  </head>
  <body>
    <nav class="nav">
        <a class="dashboardNav" href="/dashboard">Dashboard</a>
        <a class="listingNav" href="/createlisting">Listings</a>
        <a class="bookingNav" href="/makebooking?space_id=1">My Bookings</a>
        <a class="signupNav" href="/logout">Log out</a>
    </nav>
    <hr>
    <header>
      Requests page
      <h3>Welcome, please find your requests below</h3>
    </header>

    <div class="container text-center">
      <div class="row">
        <div class="col">
          <h2>Requests I've created</h2>
            <%@booker_requests.each do |request|%>

              <div class="row inner">
                <h1>Request for Property id:<%=request.space_id%><br>From: <%=request.date_from%><br>To:<%=request.date_to%></h1>
              </div>

            <%end%>

        </div>
        <div class="col">
          <h2>Requests I've received</h2>

            <%@owner_requests.each do |request|%>

              <div class="row inner">
                <h1>Request for Property id:<%=request.space_id%><br>From: <%=request.date_from%><br>To:<%=request.date_to%></h1>
              </div>

            <%end%>

        </div>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-u1OknCvxWvY5kfmNBILK2hRnQC3Pr17a+RTT6rIHI7NnikvbZlHgTPOOmMi466C8" crossorigin="anonymous"></script>
  </body>
</html>